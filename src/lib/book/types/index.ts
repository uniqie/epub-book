export type InitBookOptions = {
  portal?: HTMLElement
  // fit: boolean
  // mode
}

export type BookBasicInfoType = {
  identifier: string
  name?: string
  author?: string[]
  language?: string
  
  cover?: string
}
