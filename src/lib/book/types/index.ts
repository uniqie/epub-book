export type InitBookOptions = {
  portal?: HTMLElement
  // fit: boolean
  // mode
}

export type BookBasicInfoType = {
  id: string | number
  name?: string
  author?: string[]
  language?: string
}
